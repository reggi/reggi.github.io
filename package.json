{
  "name": "reggi-github-home",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "data:sync": "node scripts/sync-project-data.mjs",
    "preview": "vite preview",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  },
  "engines": {
    "node": ">=22"
  }
}
